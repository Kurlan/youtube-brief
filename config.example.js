/**
 * Copy this file to `config.js` (git-ignored) to preload your Google OAuth
 * client ID instead of pasting it into the app each time.
 *
 * The OAuth client ID is a public identifier for browser apps. Never put a
 * client secret or API key in this file: this app runs entirely client-side and
 * anything here is visible to anyone loading the page.
 */
window.YT_BRIEF_CONFIG = {
  googleClientId: "1234567890-abc123.apps.googleusercontent.com",
};
